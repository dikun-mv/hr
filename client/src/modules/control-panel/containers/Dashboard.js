import React from 'react';
import {Card, CardHeader, CardText} from 'material-ui/Card';

const Dashboard = props => (
  <Card>
    <CardHeader title="Добро пожаловать"/>
    <CardText>Панель управления</CardText>
  </Card>
);

export default Dashboard;
